#include <iostream>
#include<cstdio>
#include<cstring>
#include<ctime>
using namespace std;
struct tongxunlu
{
    char name[50];
    char sex;
    char number[20];
    char email[20];
    tongxunlu *next;
};
typedef tongxunlu t;
t *Add(t *head,t *pnew)//pnew指向新节点的地址
{
    clock_t start,end;
    start=clock();
    t *pwork=NULL;
    if(head==NULL)
    {
        head=pnew;
        //pwork=head;
        cin>>pnew->name;
    cin>>pnew->sex;
    cin>>pnew->number;
    cin>>pnew->email;
        pnew->next=NULL;
    }
    else
    {
    pwork=head;
    while(pwork->next!=NULL)
    {
        pwork=pwork->next;
    /*cin>>pnew->name;
    cin>>pnew->sex;
    cin>>pnew->number;
    gets(pnew->email);
    pnew->next=NULL;*/
    }
    pwork->next=pnew;
    cin>>pnew->name;
    cin>>pnew->sex;
    cin>>pnew->number;
    cin>>pnew->email;
    pnew->next=NULL;
    }
    end=clock();
    cout<<(end-start)/1000;
    return head;
}
t *Search(t *head)
{
    clock_t start,end;
    start=clock();
    char name[50];
    t *pwork;//工作指针
    pwork=head;
    if(head==NULL)
    {
        cout<<"通讯录为空！"<<endl;
        return head;
    }
    cin>>name;
    while(strcmp(name,pwork->name)!=0&&pwork->next!=NULL)
    {
        pwork=pwork->next;
    }
    if(pwork->next==NULL&&strcmp(name,pwork->name)!=0)
    {
        cout<<"未找到该联系人！";
        return head;
    }
    cout<<pwork->name;
    cout<<' ';
    cout<<pwork->sex;
    cout<<' ';
    cout<<pwork->number;
    cout<<' ';
    cout<<pwork->email;
    cout<<endl;
    end=clock();
    cout<<(end-start)/1000;
    return head;
}
t *Delete(t *head)
{
    clock_t start,end;
    start=clock();
    t *pwork1=NULL;
    t *pwork=head;
    char name[50];
    if(head==NULL)
    {
        cout<<"通讯录为空！";
        cout<<endl;
        end=clock();
        cout<<(end-start)/1000;
        return head;
    }
    else if(pwork->next==NULL)
    {
        cin>>name;
        if(strcmp(name,pwork->name)==0)
        {
            delete pwork;
            head=NULL;
            cout<<"已删除！";
            return head;
        }
        else
        {
            cout<<"未找到该联系人！";
            return head;
        }
    }
    else
    {
    cin>>name;
    if(strcmp(name,head->name)!=0)
{
    while(strcmp(name,pwork->name)!=0&&pwork->next!=NULL)
    {
        pwork1=pwork;
        pwork=pwork->next;

    if(strcmp(name,pwork->name)==0)
    {
    pwork1->next=pwork->next;
    delete pwork;
    cout<<"已删除！"<<endl;
    return head;
    }
    else if(pwork->next==NULL&&strcmp(name,pwork->name)!=0)
    {
        cout<<"未找到该联系人！";
        cout<<endl;
        return head;
    }
       }
}
    else
    {
        head=pwork->next;
        delete pwork;
        cout<<"已删除！";
        return head;
    }
}
}
t *Display(t *head)
{
    clock_t start,end;
    start=clock();
    t *pwork=head;
    if(head==NULL)
    {
        cout<<"该通讯录为空！";
        cout<<endl;
        end=clock();
        cout<<(end-start)/1000;
        return head;
    }
    while(pwork!=NULL)
    {
        cout<<pwork->name;
        cout<<' ';
        cout<<pwork->sex<<' ';
        cout<<pwork->number;
        cout<<' ';
        cout<<pwork->email;
        cout<<' ';
        cout<<endl;
        pwork=pwork->next;
    }
    end=clock();
    cout<<(end-start)/1000;
    return head;
}
int main()
{
    t *head=NULL;
    t *pnew=NULL;
    pnew=new t;
    char ch[100];
   cout<<"请选择：1.添加 2.查找 3.删除 4.显示所有记录 5.退出";
    cout<<endl;
    while(1)
    {
        //cout<<"请选择：1.添加 2.查找 3.删除 4.显示所有记录 5.退出"<<endl;
        cin.getline(ch,100);
        //cin>>ch;
        if(strcmp("5",ch)==0)
        //if(ch=='5')
        {
            break;
        }
        if(strcmp("1",ch)==0)
        //else if(ch=='1')
        {
           pnew=new t;//分配内存空间同时返回一个地址
            head=Add(head,pnew);
        }
        //cout<<head;
        if(strcmp("2",ch)==0)
        //else if(ch=='2')
        {
            head=Search(head);
        }
        if(strcmp("3",ch)==0)
        //else if(ch=='3')
        {
            head=Delete(head);
        }
        if(strcmp("4",ch)==0)
        //else if(ch=='4')
        {
            head=Display(head);
        }
        else if(strcmp("1",ch)!=0&&strcmp("5",ch)!=0&&strcmp("2",ch)!=0&&strcmp("3",ch)!=0&&strcmp("4",ch)!=0)
        //else if(ch<'1'||ch>'5'||ch>='a'&&ch<='z'||ch>='A'&&ch>='Z')
        {
            cout<<"请选择：1.添加 2.查找 3.删除 4.显示所有记录 5.退出"<<endl;
            continue;
        }
        //cout<<"xunhuan "<<endl;
        //cout<<"请选择：1.添加 2.查找 3.删除 4.显示所有记录 5.退出";
        //else
          //  continue;
       // cout<<"请选择：1.添加 2.查找 3.删除 4.显示所有记录 5.退出"<<endl;
    }
}
